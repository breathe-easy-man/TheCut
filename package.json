{
  "name": "thecut",
  "version": "1.0.0",
  "description": "Daily calorie/protein tracker with AI meal photo estimation",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "android": "cap run android",
    "open": "GDK_BACKEND=x11 cap open android",
    "test": "node test/tracker-core.test.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.3",
    "@capacitor/camera": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
